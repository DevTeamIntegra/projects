_schema-version: "3.2"
ID: prestamosgp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: prestamosgp-destination-content
  type: com.sap.application.content
  requires:
  - name: prestamosgp-destination-service
    parameters:
      content-target: true
  - name: prestamosgp-repo-host
    parameters:
      service-key:
        name: prestamosgp-repo-host-key
  - name: prestamosgp-uaa
    parameters:
      service-key:
        name: prestamosgp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: prestamosgp_html_repo_host
          ServiceInstanceName: prestamosgp-html5-srv
          ServiceKeyName: prestamosgp-repo-host-key
          sap.cloud.service: prestamosgp
        - Authentication: OAuth2UserTokenExchange
          Name: prestamosgp_uaa
          ServiceInstanceName: prestamosgp-xsuaa-srv
          ServiceKeyName: prestamosgp-uaa-key
          sap.cloud.service: prestamosgp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: prestamosgp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: prestamosgp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - prestamosgp.zip
      name: prestamosgp
      target-path: resources/
- name: prestamosgp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: prestamosgp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: prestamosgp-destination-service
    service-plan: lite
- name: prestamosgp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: prestamosgp-xsuaa-srv
    service-plan: application
- name: prestamosgp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: prestamosgp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
